#pragma once

/*
- Header file with layers
	- Each contains a pointer to the start of its block of the file for the layer map
	- First avail
	- Deleted list

OR, have cells link to each other (don't need this if we store contiguously)
*/